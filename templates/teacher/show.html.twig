<h1> Page show works ! </h1>
<p> {{t}}</p>
{% if tt|length != 0 %}
{% set total = 0 %}
<table>
    <tr>
        <td> ID </td>
        <td> NAME </td>
    </tr>
    {% for element in tt %}
    {% set total = total + element.salaire %}
    <tr>
        <td>{{element.id}}</td>
        <td>{{element.name | upper }}</td>
        <td>{{element.salaire}}</td>
        <td><a href="{{path('app_details', {'id': element.id} )}}"> Détails </td>
    </tr>
    {%endfor%}
</table>
{% else %}
<p>Pas d'elements </p>
{% endif %}
{% if tt is empty %} <p> Pas d'elements </p>
{% endif %}

{% if 5 not in [1,2,3] %}
5 non present <br>
{% endif %}
somme des salaire = {{total}}

<img src="/images/img1.png" width="300" height="300">
<link re='stylesheet' href='/css/'>
<script src="/js/">
